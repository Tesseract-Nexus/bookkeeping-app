apiVersion: v1
kind: ConfigMap
metadata:
  name: bookkeeping-config
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  LOG_LEVEL: "info"
  LOG_FORMAT: "json"
  ENVIRONMENT: {{ .Values.global.env | default "dev" | quote }}
